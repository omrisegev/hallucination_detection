# Reviewer guide

Start with `DECISION.json`, then `PAIRWISE_COMPARISONS.csv`, `PER_CELL_METRICS.csv`, `SEED_STABILITY.csv`, and `WHOLE_SEARCH_NULL.json`. Verify `SCORE_FREEZE_MANIFEST.json` predates the label sidecars. The active arm roster is exactly B0/B1/B2/B3.
