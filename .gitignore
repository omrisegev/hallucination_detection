# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Jupyter checkpoints
.ipynb_checkpoints/

# Large data / model files
*.pkl
*.bin
*.safetensors
*.pt
*.pth
*.h5
*.ckpt

# Drive / inference caches (large, not for git)
inference_cache.pkl
phase*_results.pkl
*_summary.json

# Environment
.env
*.env
venv/
.venv/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Keys / credentials
keys.txt
*.pem
*token*
# Live sbatch carries a hardcoded HF_TOKEN — only the .template is tracked
cluster/submit_inference.sbatch

# Generated plots (large, reproducible)
*.png
*.pdf
*.svg

# Older / result notebooks — kept local only (may contain API tokens in outputs)
*_res.ipynb
View_Optimizer.ipynb
CoT_EPR_Ensemble*.ipynb
EDIS_Replication*.ipynb
EPR_Validation*.ipynb
Multimodel_EPR_Ensemble*.ipynb
Multiview_EPR_*.ipynb
Multiview_Hallucination_Detection*.ipynb
Temperature_EPR_Ensemble*.ipynb
Unified_EPR_Ensemble*.ipynb
Spectral_Analysis_Phase1*.ipynb
Spectral_Analysis_Phase2*.ipynb
Spectral_Analysis_Phase3*.ipynb
# Phase10+ accidentally matched by Phase1* above — re-include explicitly
!Spectral_Analysis_Phase10*.ipynb
!Spectral_Analysis_Phase11*.ipynb

# Claude Code local settings
.claude/

# Raw conversation logs (private)
Con_with_*.txt
*.txt

# Windows
$RECYCLE.BIN/

# External baseline repos cloned locally for inspection (not part of this repo)
baselines/lapeigvals/
baselines/losnet/

# Phase 12+ notebooks — re-include (would otherwise match Phase1* above)
!Spectral_Analysis_Phase12*.ipynb
!Spectral_Analysis_Phase13*.ipynb
!Spectral_Analysis_Phase14*.ipynb

# Local runner — downloaded Drive pkls (not committed, re-download from Drive)
local_cache/

# AIRCC replication-grid pkls fetched from the cluster (canonical copy lives on $SHARED/results/repgrid, ~1.5 GB — not for git)
cache/repgrid/

# Generated HTML reports (reproducible from archive.jsonl)
results/runs/
results/report_*.html

# Phase 15+ notebooks — re-include (would otherwise match Phase1* above)
!Spectral_Analysis_Phase15*.ipynb

# scripts/build holds notebook generators, not Python dist output — re-include
# (the `build/` rule above is meant for packaging artifacts)
!scripts/build/
