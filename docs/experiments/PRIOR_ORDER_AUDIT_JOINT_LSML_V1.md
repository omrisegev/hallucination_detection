# Prior-order audit: Joint L-SML v1

Date: 2026-09-04  
Worktree: `/Users/osegev/Desktop/hallucination_detection/local_cache/worktrees/og_sml_agent_b_v1`  
Branch: `codex/og-sml-agent-b-v1`  
Base commit: `250e092e1a0f5b2e460e2fd0221bcbded28069dc`

## Authority and relationship to T0

The current goal explicitly accepts the negative T0 result, deletes Remark 6,
and authorizes a narrower disjoint-group `Joint L-SML` program.  T0 remains an
unaltered negative result for the earlier overlapping explanation.  This
follow-up does not reinterpret T0 as a pass and does not implement overlap.

The active request authorizes:

1. a label-free donor-score rank-stability reanalysis of the 18 frozen C-v2
   lanes under condition targets `1e2`, `1e3`, and `1e4`;
2. a joint estimator for the existing disjoint L-SML covariance model;
3. identifiability-driven pruning, absolute anchor-based orientation, constrained
   LOAO-consensus group discovery, three weight maps, structural comparisons,
   and external Agent-A registries.

## Repository-order mapping

- `CLAUDE.md`: work remains in the isolated Agent-B worktree and is hash-bound
  before execution.  No commit, push, package install, cluster work, or external
  mutation is authorized.
- `PROGRESS.md`: existing PB/PRM labels and Phase 3 remain closed for new efficacy
  selection.  This run loads only the already sanitized target-free donor arrays
  and computes no AUROC, F1, AURC, first-error, or ranking metric.
- `HISTORY.md`: continuous L-SML remains the historical fallback/reference.  Its
  historical detection gains are not promoted to official localization evidence.
- `Research_Directions.md`: prior negative conclusions for graph smoothing on the
  step axis, DUFS, residual-guided efficacy selection, PGRD, rank transform, Katz,
  and additional monotone transformations remain unchanged.  The present method
  changes the estimator and weight map inside the fixed L-SML model only.
- C-v2 is read-only lineage.  Its source and artifacts are not edited.

## Forbidden actions

- No labels or response-score arrays may be materialized.
- No benchmark outcome metric or fused PB/PRM score array may be persisted.
- No overlap, lag grouping, OG-SML T1/T2, or external innovation columns.
- No new scoring candidate, multiplicity slot, promotion claim, commit, or push.

Any violation fails the run.

