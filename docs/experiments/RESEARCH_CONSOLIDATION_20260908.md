# Research consolidation — user-approved scope, 2026-09-08

## Current stage: finish existing obligations, then return the reflection

Omri explicitly authorized implementation after the earlier pause. Complete the
historical Joint extension first, then the entire frozen sampling run. Verify
checkpoints, source hashes and absence of duplicate processes before resuming.
Keep existing drivers, manifests, populations, variants and invocation caps.
No new inference, cluster migration, feature banks, windows, graphs or lambda search.
Do not edit frozen dependencies. Preserve all existing results and code.

Independently review Claude Step331 using frozen peaks and raw entropy: reproduce
GMM20.00%, nested31.31%, quantile31.16%, every entropy threshold, source-group
exclusion and validity. Add the missing entropy-mean q0.3 versus GMM interval,
10,000 canonical-group draws; compare transfer on the same destination cells.
This review is separate from Claude's immutable report. No method selection.

Deliver one Hebrew HTML reflection and a machine-readable evidence ledger,
then report back BEFORE starting a new improvement experiment. The ledger covers
both agents, features/normalization, feature fusion, graphs/conditioning,
window sampling, trajectory processing, gate/location, and evaluation engineering.
Keep the original historical number and protocol separate from corrected matched
results. Every result needs population, metric, fitting/label access, coverage,
uncertainty, source, adoption decision and remaining question. Distinguish pilot,
unfinished, reviewed, infrastructure adoption, candidate, not promoted and deferred.
Do not close a research family from one failed implementation.

Update HISTORY, PROGRESS and Research_Directions with actions AND their reasons.
Incorporate Claude's completed comparators without duplicate execution. Missing
historical leaders (including Mind the Gap until a verified v3 result exists)
remain visible; never claim the complete historical benchmark is finished while
required methods lack compatible results.

## Fixed direction after the stage boundary

ProcessBench first; retain PRMBench pooled and within-answer panels and coverage.
Freeze the simple candidate: raw mean entropy, q=0.3. Fusion fitting remains
within the answer, but quantile calibration borrows other answers without labels.
Detector/q selection used development outcomes; it is not fully label-free
method selection or untouched confirmation.

Next experiment (not started by the completion stage): shared saved dual__iu
entropy-q0.3 fold thresholds on entropy_parent, dual equal, dual IU, Joint100,
Joint100 graph, Joint100 permuted graph, and the already tested IU/Joint-graph
mean recipe. Keep peaks and failures unchanged, show native GMM alongside.
Primary contrasts: IU minus entropy, existing mean minus IU. Graph contrasts
are mechanistic diagnostics, not a new winner search.

Following experiment (separate stage): for IU and entropy only, compare maximum
token risk per official step against mean of the largest min(10, step length)
token risks. Preserve token risks, mapping, shared gate, and PRMB ranking.
Argmax ties select the first step. Do not tune the number10.

For each of those stages: 10,000 paired canonical-source-group draws, 97.5%
intervals for its two primary contrasts; condition on fixed fitted predictions.
Report PB all8, Q4/Q8, cells, clean accuracy, exact first-error accuracy,
early/late peaks, suppressed correct peaks and all failures. Preserve PRMB
pooled/within metrics and common-valid denominators. Return at each stage boundary;
no automatic additional sweep if the result is negative.

## Deferred

LOCA, Diverging Flows, KalmanNet, new adaptive sampling and historical24 transfer
remain backlog. Untouched confirmation requires a candidate lock and source
overlap audit; MR-GSM8K is a candidate, not yet a verified untouched test.
No clear winner is promised. If fusion does not beat entropy, report that plainly.
