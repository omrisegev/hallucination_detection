# Fixed-bank Joint readout intervention, 2026-09-17

Step401 was verified progress: the complete redundancy/noise stress refuted
practical preservation by the95% alternating selector. The fixed hierarchical
readout can give a weak pure-noise group large absolute weight. That observation
does not establish causation. This stage changes ONLY the readout, not discovery,
selected support, optimizer, fitting data, gate, labels or BOCPD construction.

One predeclared candidate: the existing Joint model-inverse map
`regularized_joint_map_weights(None, model_covariance, v, mode='liu', lam=0,
target_condition=1000)`, externally sign-oriented by the unchanged H1 rule.
There is no graph at lambda0. This is reuse of the historical native Joint map,
not a newly invented algorithm or a claim that the global factor is correctness.
Its covariance contains the global factor, block-local factors and clipped
diagonal residual variance; save clipping/ridge diagnostics explicitly.

Banks: original B51 BOCPD Top10, B66 exact duplicates, B66 independent noise.
Both full support and frozen automatically selected support. Reproduce each
accepted historical fit with the original seed plus its deletion-path index;
require the reconstructed hierarchical weights to match frozen weights before
using its model-inverse map. Do not alter selection after seeing new scores.
Save fitted loadings/covariances to allow subsequent audits without refitting.

Unsuccessful original fits remain explicit H1 fallbacks for both heads. This
readout-only intervention cannot rescue an initial fit that did not converge.
Report native coverage and complete13769-answer end-to-end scores separately.
All weights fit other training-source folds (hybrid, not answer-only). Same
non-digit gate: answer Tail15 Top10 mean, cell midrank>=.33. PRMB within ungated.

Five primary paired contrasts, each PB and PRMB within,10000 source-group
bootstrap draws with Bonferroni99.5% intervals: candidate auto versus historical
hierarchical auto separately on each bank (three), and candidate auto under each
stress versus candidate auto original bank (two). Full-support heads and existing
Continuous/equal/innovation5/historicalBOCPD references are descriptive controls.
Retain practical preservation margins PB-1pp/within-.002, full native coverage;
do not call reduced sensitivity with poor base quality a solution. No condition
grid, added seeds, correctness-driven sign, or threshold tuning in this stage.

Scope: one full matched readout comparison, independent metric/weight/system
audit and mechanism interpretation. Readout improvement alone is insufficient
for the broader goal: integrated inclusion must preserve useful localization
under extra features, including fit coverage, against the strong references.
