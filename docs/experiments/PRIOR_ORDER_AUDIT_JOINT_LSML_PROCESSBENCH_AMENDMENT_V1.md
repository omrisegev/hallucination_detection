# Prior-order audit: Joint L-SML ProcessBench amendment v1

Status: `PASS_WITH_EXPLICIT_LATEST_USER_OVERRIDE`.

## Earlier binding rules

The parent Joint L-SML protocol required an all-eight structural pass and
therefore recorded `STRUCTURAL_NO_SCORE` when Qwen3-4B/MATH had no admissible
partition. The project research ledger also closed further scoring-method search
on the already-open 3,400-question ProcessBench population. Those rules were
correctly followed by the parent run: no PB score arrays were written and no PB
labels were accessed.

The parent PRMBench result is already open and is `HARM`. Its registries, score
freeze, evaluation, report, and independent audits remain immutable.

## Latest authority

After seeing the no-score disposition, the user explicitly insisted on running
and comparing ProcessBench. That instruction authorizes exactly one versioned,
retrospective PB-only coverage policy: frozen Joint L-SML on the seven parent-
admissible cells and the existing `G=[]` flat-SML alias on the sole structurally
blocked cell. It does not authorize a group/K/reducer/feature sweep, a second
fallback, post-label repair, promotion, or a generalization claim.

The amendment is registered after the PRMBench result was opened but before
ProcessBench labels are parsed. It is therefore labeled
`POST_PRM_OPENED__PB_LABEL_FREE_AT_REGISTRATION`, uses one new candidate policy
plus the same three controls, and records cumulative exposure.

## Non-contradiction conditions

The override is narrow and valid only if all of the following hold:

1. Parent artifacts and seven parent weight hashes remain byte-identical.
2. The sole fallback is fixed before PB labels and is exactly the flat-SML alias
   up to the allowed global sign gauge.
3. All eight score arrays are frozen and independently replayed before labels.
4. The fixed top-10 reducer, folds, bootstrap, metric, and decision rule are
   hash-bound before evaluation.
5. The result is retrospective development evidence only; a positive result
   cannot promote the method or serve as fresh confirmation.

Under these conditions the latest instruction supersedes the earlier no-scoring
boundary only for this one ProcessBench evaluation. All other prior restrictions
remain active.
