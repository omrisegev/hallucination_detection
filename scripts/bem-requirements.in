# Python 3.11 is recommended. tensorflow-text must match tensorflow's release.
tensorflow
tensorflow-hub
tensorflow-text
# Optional GPU support on macOS: install tensorflow-metal only after checking
# that its release is compatible with the TensorFlow version resolved above.
# BEM inference works on CPU and uses exactly the same model and tokenizer.
